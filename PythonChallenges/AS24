from datetime import date
def day_calculator(projectstart, projectend):
    calculation = projectend - projectstart
    return calculation.days

print("The time it took for your project to finish is " + str(day_calculator(date(2022, 10, 6), date(2023, 7, 20))) + " days")
